services:
  - type: web
    name: la-fecha-sim-engine
    runtime: python
    plan: free
    rootDir: services/sim-engine
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
